<div class="content p-1">
    <div class="list-group-item">
        <div class="d-flex">
            <div class="mr-auto p-2">
                <h2 class="display-4 titulo">Listar de Usuario</h2>
            </div>
        </div>
        <hr>
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered">
                <thead>
                    <tr>
                        <!--<th>ID</th>-->
                        <th>Nome</th>
                        <th class="d-none d-sm-table-cell">E-mail</th>
                        <th class="d-none d-sm-table-cell">Tipo</th>
                        <th class="text-center">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each usuarios.docs}}
                    <tr>
                        <!--<th>{{_id}}</th>-->
                        <td>{{nome}}</td>
                        <td class="d-none d-sm-table-cell">{{email}}</td>
                        <td class="d-none d-sm-table-cell">{{tipoUsuario}}</td>
                       
                        <td class="text-center">
                            <span class="d-none d-md-block">
                                <a href="/usuario/edit-usuario/{{_id}}" class="btn btn-outline-primary btn-sm">Visualizar</a>
                                <a href="/usuario/del-usuario/{{_id}}" class="btn btn-outline-danger btn-sm">Excluir</a>
                            </span>
                            <div class="dropdown d-block d-md-none">
                                <button class="btn btn-primary dropdown-toggle btn-sm" type="button" id="acoesListar"
                                    data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Ações
                                </button>
                                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="acoesListar">
                                    <a class="dropdown-item" href="/usuario/cadastro-usuario/{{_id}}">Visualizar</a>
                                    <a href="/usuario/del-usuario/{{_id}}" class="btn btn-outline-danger btn-sm">Excluir</a>
                                </div>
                            </div>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <nav aria-label="paginacao">
                <ul class="pagination pagination-sm justify-content-center">
                    {{#if usuarios.hasPrevPage}}
                    <li class="page-item">
                        <a class="page-link" href="/usuario/vis-usuario" tabindex="-1">Primeira</a>
                    </li>
                    <li class="page-item"><a class="page-link"
                            href="/usuario/vis-usuario?page={{usuarios.prevPage}}">{{usuarios.prevPage}}</a></li>
                    {{else}}
                    <li class="page-item disabled">
                        <a class="page-link" href="#" tabindex="-1">Primeira</a>
                    </li>
                    {{/if}}

                    <li class="page-item active">
                        <a class="page-link" href="#">{{usuarios.page}}</a>
                    </li>

                    {{#if usuarios.hasNextPage}}
                    <li class="page-item ">
                        <a class="page-link" href="/usuario/vis-usuario?page={{usuarios.nextPage}}">{{usuarios.nextPage}}</a>
                    </li>

                    <li class="page-item ">
                        <a class="page-link" href="/usuario/vis-usuario?page={{usuarios.totalPages}}">Proxima</a>
                    </li>
                    {{else}}
                    <li class="page-item disabled">
                        <a class="page-link" href="#">Última</a>
                    </li>
                    {{/if}}
                </ul>
            </nav>

        </div>
    </div>
</div>